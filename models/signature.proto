syntax = "proto3";

package models;

option go_package = "github.com/cjhouser/washere/models";

service Signature {
    rpc Get (SignatureRequest) returns (stream SignatureResponse) {}
}

message SignatureRequest {}

message SignatureResponse {
    uint64 id = 1;
    string signature = 2;
}